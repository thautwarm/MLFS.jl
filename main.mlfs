module main
val main : i64
let main = 1

let x = 1
check main as main

val unit : Type @unit
let unit = unit

val println : forall a. a -> unit
let println = extern "println"

val eq : forall a. (a, a) -> bool
let println = extern "=="

let _ = println (
  if eq (1, 2)
  then 1
  else 2
)
